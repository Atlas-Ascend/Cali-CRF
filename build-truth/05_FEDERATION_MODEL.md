# 05 — Federation Model

Federation is capability-driven, evidence-aware, and deterministic for equivalent inputs.

Selection factors:
- required capabilities
- runtime health
- locality constraints
- authority ceiling
- current load
- packet priority
- evidence requirement
- failure history
- runtime class bias

The scheduler produces an explainable assignment record for every dispatch.
