# 06 — Packet Contract

Packet OS is the atomic work substrate.

CALI-CRF requires each packet to contain identity, objective, requested capabilities, authority, expected evidence class, and correlation metadata. Inputs, expected outputs, priority, and explicit failure route are strongly recommended.

Packets are immutable after dispatch except through an explicit superseding or repair packet.
