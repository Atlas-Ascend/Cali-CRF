# 14 — Deployment

Deployment targets may include local EDEN nodes, Render API/worker surfaces, operator phones, and Hypernet hardware.

The control plane remains portable. Configuration is environment-driven. No provider-specific assumption becomes the canonical domain model.

Deployment requires health checks, schema validation, runtime registry validation, and receipt smoke tests.
