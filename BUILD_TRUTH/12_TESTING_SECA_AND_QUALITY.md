# 12 — Testing, SECA, and Quality

Tests: capability advertisement accuracy, denied undeclared action, bounded file/tool/model calls, timeout/cancel, heartbeat loss, idempotent retry where safe, receipt integrity, attestation freshness, offline/reconnect behavior.

SECA validates representative end-to-end local execution.