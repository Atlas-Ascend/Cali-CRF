# 07 — Packet OS Integration

Every local execution enters through a packet-compatible request. packet_id and objective/constraints survive local execution. Partial/failing work returns explicit state and evidence so Packet OS can retry, reroute, block, or escalate.