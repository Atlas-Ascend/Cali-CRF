# 04 — Architecture

Components: node capability adapter, local policy evaluator, task executor, tool/model adapters, filesystem boundary, telemetry/heartbeat client, attestation/receipt emitter, retry/recovery controller.

Local actions remain traceable to packet and route ids.