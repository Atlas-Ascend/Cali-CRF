# 11 — Security, Governance, and Permissions

Least privilege and locality are mandatory. Paths/tools/models must be explicitly declared or policy-approved. Sensitive inventory is redacted outside trusted channels. Destructive filesystem/shell/device actions require explicit governed capability and authorization.

Remote callers cannot widen local permissions.